<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Logger
{
    public static function log($msg)
    {
        // $path = 'E:\Program Files\wamp\www\truelab\log\\' . date('Ymd') . '.log';

        // $f = fopen($path, 'a+');
        // fwrite($f, date('[H:i:s]: ') . $msg . "\n");
        // fclose($f);
    }
}